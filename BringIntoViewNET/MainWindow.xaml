<Window
    x:Class="BringIntoViewNET.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BringIntoViewNET"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Path=WindowTitel, FallbackValue=WindowTitel}"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.Icon>
        <BitmapImage UriSource="pack://application:,,,/Resources/Picture/ApplicationIcon.png" />
    </Window.Icon>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="5,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Select Item to scroll to" />

            <ComboBox
                x:Name="SelectionList"
                Grid.Column="1"
                DisplayMemberPath="Item1"
                ItemsSource="{Binding}"
                SelectionChanged="OnSelectedItemChanged" />

        </Grid>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Click on any of the buttons when they are not 100% visible and the scrollviewer will auto scroll... " />
            <CheckBox x:Name="DisableAutoScroll" Content="Check this to disable the autoscroll on the scrollviewer" />
        </StackPanel>


        <ScrollViewer Grid.Row="2">
            <ListBox x:Name="List" ItemsSource="{Binding}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button
                            Width="200"
                            Height="150"
                            Background="{Binding Item2}">

                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="25"
                                FontWeight="Bold"
                                Foreground="White"
                                Text="{Binding Item1}" />

                        </Button>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Window>
